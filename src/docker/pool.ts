//=============================================================================
// W2Wizard, Amsterdam @ 2018-2023
// See README and LICENSE files for details.
//=============================================================================

// A Pool of docker containers using a single socket for communication to the
// docker daemon.

import DockerSocket from "./socket";

//=============================================================================

/** A pool of docker containers. */
export default class Pool {
	constructor() {

	}
}
